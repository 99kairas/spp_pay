import 'package:flutter/material.dart';

class AdminTambahPembayaran extends StatelessWidget {
  const AdminTambahPembayaran({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Tambah Pembayaran'),
    );
  }
}