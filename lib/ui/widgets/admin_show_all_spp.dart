import 'package:flutter/material.dart';

class AdminShowAllSpp extends StatelessWidget {
  const AdminShowAllSpp({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Lihat Semua Spp'),
    );
  }
}
