import 'package:flutter/material.dart';

class AdminTambahSpp extends StatelessWidget {
  const AdminTambahSpp({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Tambah SPP'),
    );
  }
}
